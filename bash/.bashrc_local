#!/bin/bash

#
# This is template .bashrc_local. It was ment to be used
# as local-machine customizable bash setting.
#

BASH_COMPL="/usr/local/etc/profile.d/bash_completion.sh"
[ -f ${BASH_COMPL} ] && source ${BASH_COMPL}
